# Production: VITE_API_URL sengaja dikosongkan.
# Frontend akan auto-detect server via prioritas:
#   1. Tauri invoke -> Rust get_detected_api_url (scan LAN)
#   2. Cache localStorage
#   3. JS subnet scan (192.168.x.x)
#   4. Prompt user manual
# TIDAK ada fallback ke localhost di mode production.
VITE_API_URL=
